services:
  api:
    build: .
    ports:
    - "8000:8000"
    - "5173:5173"
    extra_hosts:
    - "host.docker.internal:host-gateway"
    volumes:
    - ./models:/app/models:ro
    environment:
    - MODEL_PATH=/app/models/attitude_model.keras